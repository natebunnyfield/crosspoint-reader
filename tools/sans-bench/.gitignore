# Generated: ~900 KB of base64-inlined device renders and font subsets.
# Rebuild with build_page.py after re-running the harness; the hosted copy is
# the one people read. The inputs (JSON + measurements) are committed.
sans-bench.html
