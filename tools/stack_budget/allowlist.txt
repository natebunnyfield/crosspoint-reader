# Stack frame budget allowlist -- pre-existing frames permitted above the threshold.
#
# Format, one frame per line:
#
#   <budget-bytes> <repo-relative-file> <demangled signature exactly as -fstack-usage prints it>  # reason
#
# The reason after '#' is MANDATORY. An entry without one is rejected and the
# gate fails -- an exemption nobody had to justify is how a budget dies.
#
# The budget is the frame's MEASURED size at the time it was added. It is a cap,
# not a floor: if the frame grows past it the gate fails as a regression. So
# ratcheting is just "re-run, then lower the numbers the tool reports as slack".
#
# Keyed on file + signature, deliberately NOT on line number: line numbers move
# whenever anything above the function is edited, which would rot this file on
# every commit.
#
# SEEDED: 2026-07-29 against git 9346cdb7 ("feat: 6-week CR+US holiday calendar
# sleep screen"), env=default, riscv32-esp-elf-gcc, -Os, threshold 512.
# 456 .su files, 9869 frames total, 1854 first-party. These 21 are every
# first-party frame over 512 bytes in that tree -- no invented entries, and no
# frame is listed at anything other than the size actually measured.
#
# CORRECTION 2026-07-29: the original seed said 20 and missed
# BidiUtils::computeVisualWordOrder, because stack_budget.py exempted all of
# lib/MiniBidi/ as vendored when only minibidi.c/.h actually is. The exemption is
# now narrowed and that 21st frame is listed below.
#
# CAVEAT for whoever merges this: the seed was taken while other work was in
# flight in src/network/, src/activities/ and lib/. Re-run with --fresh once the
# tree settles; the tool will report entries that are now stale (frame gone) or
# obsolete (frame under threshold) so they can be deleted.
#
# Verified dominant contributor means: read the function and found the local
# that accounts for most of the frame, cited by file:line. "Not yet attributed"
# means the frame was measured but the cause was not investigated -- an honest
# TODO, not a claim.

# --- A recurring idiom, not 7 unrelated bugs: a single ~500-byte char local. ---
# `char name[500]` / `char output[512]` appears 20 times across src/ and lib/
# (grep for it). Each one alone is 2x the 256-byte Resource Protocol limit.
1008 src/activities/boot_sleep/SleepActivity.cpp void SleepActivity::renderCustomSleepScreen() const  # char name[500] at SleepActivity.cpp:118 dominates; pre-existing debt
784 src/network/CrossPointWebServer.cpp void CrossPointWebServer::handleGetSettings() const  # char output[512] at CrossPointWebServer.cpp:1166; runs on loopTask (see main.cpp:314-318)
704 src/activities/util/BmpViewerActivity.cpp void BmpViewerActivity::loadSiblingImages()  # char name[500] at BmpViewerActivity.cpp:35; pre-existing debt
688 src/network/CrossPointWebServer.cpp void CrossPointWebServer::handleGetOpdsServers() const  # char output[512] at CrossPointWebServer.cpp:1335; loopTask
672 src/network/CrossPointWebServer.cpp void CrossPointWebServer::handleFileListData() const  # char output[512] at CrossPointWebServer.cpp:490; loopTask
672 src/network/WebDAVHandler.cpp void WebDAVHandler::handlePropfind(WebServer&)  # char name[500] at WebDAVHandler.cpp:228; loopTask (main.cpp:314-318 cites WebDAVHandler frames as the reason for the 16K loopTask)
640 src/network/CrossPointWebServer.cpp void CrossPointWebServer::scanFiles(const char*, const std::function<void(FileInfo)>&) const  # char name[500] at CrossPointWebServer.cpp:424; loopTask

# --- Deliberate, documented tradeoff: keep on the stack. ---
640 lib/Epub/Epub/hyphenation/LiangHyphenation.cpp std::vector<unsigned int> liangBreakIndexes(const std::vector<CodepointInfo>&, const SerializedHyphenationPatterns&, const LiangWordConfig&)  # AugmentedWord's fixed arrays (LiangHyphenation.cpp:97-102) + scores[70]; the comments at :350-352 and :367-368 choose stack over heap on purpose, to avoid fragmenting the heap across hundreds of words per page

# --- Measured, cause not yet attributed. Triage these before lowering the threshold. ---
816 lib/KOReaderSync/ProgressMapper.cpp static CrossPointPosition ProgressMapper::toCrossPoint(const std::shared_ptr<Epub>&, const SavedProgressPosition&, GfxRenderer&, int, int, int)  # not yet attributed; no large local array in the function head
768 lib/Epub/Epub/parsers/ChapterHtmlSlimParser.cpp static void ChapterHtmlSlimParser::startElement(void*, const XML_Char*, const XML_Char**)  # not yet attributed; expat per-element callback, 3 std::string locals at :358-360 do not explain 768
768 lib/Xtc/Xtc.cpp bool Xtc::generateThumbBmp(int) const  # not yet attributed
624 src/network/HttpDownloader.cpp HttpDownloader::DownloadError {anonymous}::runGetWolf(const std::string&, const std::string&, const std::string&, Sink&)  # not yet attributed; sits under every OTA/OPDS/font download
592 lib/Epub/Epub.cpp bool Epub::parseContentOpf(BookMetadataCache::BookMetadata&, bool)  # not yet attributed
592 lib/Epub/Epub/BookMetadataCache.cpp bool BookMetadataCache::buildBookBin(const std::string&, const BookMetadata&)  # not yet attributed
592 src/components/themes/lyra/LyraTheme.cpp virtual void LyraTheme::drawRecentBookCover(GfxRenderer&, Rect, const std::vector<RecentBook>&, int, bool&, bool&, bool&, std::function<bool()>) const  # not yet attributed; render task
576 src/activities/reader/KOReaderSyncActivity.cpp virtual void KOReaderSyncActivity::render(RenderLock&&)  # not yet attributed; render task
576 src/components/themes/roundedraff/RoundedRaffTheme.cpp virtual void RoundedRaffTheme::drawRecentBookCover(GfxRenderer&, Rect, const std::vector<RecentBook>&, int, bool&, bool&, bool&, std::function<bool()>) const  # not yet attributed; render task
560 src/activities/boot_sleep/SleepActivity.cpp void SleepActivity::renderCoverSleepScreen() const  # not yet attributed
544 lib/KOReaderSync/ProgressMapper.cpp static std::string ProgressMapper::generateXPath(const std::shared_ptr<Epub>&, int, float)  # not yet attributed
544 src/activities/settings/FontDownloadActivity.cpp bool FontDownloadActivity::fetchAndParseManifest()  # not yet attributed

# --- Recovered by narrowing the lib/MiniBidi/ exemption (see stack_budget.py). ---
# minibidi.c/.h is vendored; BidiUtils.cpp/.h is ours, so it belongs in the budget.
608 lib/MiniBidi/BidiUtils.cpp bool BidiUtils::computeVisualWordOrder(const char*, unsigned int, std::vector<BidiUtils::VisualWord>&, BidiUtils::BidiBaseDir)  # not yet attributed; on the RENDER task (8K) via GfxRenderer text layout, so worth attributing before ratcheting below 608
