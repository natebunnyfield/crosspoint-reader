Host-side gtest unit tests for crosspoint-reader.

Build and run:

  cmake -S test -B build/test
  cmake --build build/test
  ctest --test-dir build/test --output-on-failure -j

Run a single suite directly:

  cmake --build build/test --target StreamingJsonParserTest
  build/test/streaming_json_parser/StreamingJsonParserTest --gtest_filter='*'

Google Test is fetched via CMake FetchContent on first configure; the pinned
version lives in test/CMakeLists.txt.

Shared pieces:

  cmake/HostThemeStack.cmake  crosspoint_add_theme_stack(target repo_root) links
                              the REAL UITheme + BaseTheme/Lyra/Lyra3Covers/
                              LyraSix chain host-side. Needed by any suite whose
                              code reads GUI — which includes activity onEnter/
                              loop paths, not just render().
  host_stubs/                 The headers that chain needs and only that:
                              HalClock.h, HalPowerManager.h (their real versions
                              reach ESP-IDF) and WString.h (FsHelpers.h wants
                              Arduino's String). Suite-specific seams — HalGPIO,
                              HalStorage — stay in each suite's own stubs/.

Running from a git worktree:

Two inputs live in the primary checkout and not in a worktree, and configure
falls back to the primary checkout for both (it says so in the configure log):

  .pio/libdeps  ArduinoJson, needed by activity_input and sleep_screen. It
                comes from PlatformIO's lib_deps rather than the repo, so it
                appears only after a `pio run`.
  fs_           the SD-card font tree, gitignored. Without it
                font_switch_churn and system_font SKIP nine tests and the
                suite still reports 100% pass — the failure mode this
                fallback exists to prevent.

Override the SD tree explicitly with CROSSPOINT_TEST_SD=/path/to/fs_ when
running a suite by hand; ctest already carries the resolved one.

After switching branches, regenerate the i18n headers:

  python3 scripts/gen_i18n.py lib/I18n/translations lib/I18n/

I18nKeys.h / I18nStrings.h / I18nStrings.cpp are generated and gitignored, so
they do NOT follow a branch switch. Picking up a branch that added a string
gives a compile error naming the new key, e.g.

  error: no member named 'STR_CLAUDE_PROMPT_HINT' in 'StrId'

which looks like the test tree is broken rather than merely stale.
